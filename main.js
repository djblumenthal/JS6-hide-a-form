$(document).on('ready', function() {
  //set value in input fields to text in profile sections 
  $('#form-name').val(
  //when submit/"save button" clicked:
  $('#profile-edit').on('submit', function(e){
  	
  	//prevent page from refreshing
  	e.preventDefault;
  	
  	//take values from input fields and save to corresponding profile sections

  })
});